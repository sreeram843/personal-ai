# Cloud inference configuration — copy to .env.cloud and fill in your values.
# Usage: docker compose --profile cloud-chat -f docker-compose.yml -f docker-compose.cloud.yml --env-file .env.cloud up -d
#
# NEVER commit the filled-in version of this file.
#
# Ollama runs locally for embeddings only (nomic-embed-text). Deploy scripts pull
# this model automatically; for manual setup: make pull-models-cloud

# ─────────────────────────────────────────────────────────────────────────────
# Provider selection — uncomment ONE block
# ─────────────────────────────────────────────────────────────────────────────

# ── GMI Cloud ─────────────────────────────────────────────────────────────────
# Free: meta-llama/Llama-3.1-8B-Instruct ($0/1M tokens)
# Paid: Qwen/Qwen2.5-7B-Instruct, Qwen/Qwen2.5-3B-Instruct (~$0.10/1M tokens)
# Get key: https://console.gmicloud.ai → Settings → API Keys
#
# LLM_CLOUD_BASE_URL=https://api.gmi-serving.com
# LLM_CLOUD_API_KEY=your-gmi-api-key-here
# LLM_CLOUD_DEFAULT_MODEL=meta-llama/Llama-3.1-8B-Instruct
# LLM_CLOUD_PLANNER_MODEL=Qwen/Qwen2.5-3B-Instruct
# LLM_CLOUD_SYNTHESIZER_MODEL=Qwen/Qwen2.5-7B-Instruct
# LLM_CLOUD_REVIEWER_MODEL=Qwen/Qwen2.5-3B-Instruct
# LLM_CLOUD_WRITER_MODEL=meta-llama/Llama-3.1-8B-Instruct
# LLM_CLOUD_TIMEOUT=60.0

# ── Groq (very fast inference, generous free tier) ────────────────────────────
# Tiered: 8B for planner/reviewer, 70B for synthesizer/writer.
# Enable models at: https://console.groq.com/settings/project/limits
# Get key: https://console.groq.com → API Keys
#
# LLM_CLOUD_BASE_URL=https://api.groq.com/openai
# LLM_CLOUD_API_KEY=your-groq-api-key-here
# LLM_CLOUD_DEFAULT_MODEL=llama-3.1-8b-instant
# LLM_CLOUD_PLANNER_MODEL=llama-3.1-8b-instant
# LLM_CLOUD_SYNTHESIZER_MODEL=llama-3.3-70b-versatile
# LLM_CLOUD_REVIEWER_MODEL=llama-3.1-8b-instant
# LLM_CLOUD_WRITER_MODEL=llama-3.3-70b-versatile
# LLM_CLOUD_TIMEOUT=30.0
# ENABLE_TOOL_AGENT=false

# ── DeepSeek (OpenAI-compatible, competitive pricing) ─────────────────────────
# Models: deepseek-chat (general), deepseek-reasoner (slower, more reasoning).
# Newer aliases: deepseek-v4-flash, deepseek-v4-pro — see https://api-docs.deepseek.com
# Get key: https://platform.deepseek.com → API Keys
#
# LLM_CLOUD_BASE_URL=https://api.deepseek.com
# LLM_CLOUD_API_KEY=your-deepseek-api-key-here
# LLM_CLOUD_DEFAULT_MODEL=deepseek-v4-flash
# LLM_CLOUD_PLANNER_MODEL=deepseek-v4-flash
# LLM_CLOUD_SYNTHESIZER_MODEL=deepseek-v4-pro
# LLM_CLOUD_REVIEWER_MODEL=deepseek-v4-flash
# LLM_CLOUD_WRITER_MODEL=deepseek-v4-pro
# LLM_CLOUD_TIMEOUT=180.0
# ENABLE_TOOL_AGENT=false
# Orchestrated workflow runs planner → synthesizer → reviewer → writer; use ≥180s for v4-pro.

# ── Together AI ($25 free credit on signup) ───────────────────────────────────
# Models: meta-llama/Llama-3.1-8B-Instruct-Turbo, Qwen/Qwen2.5-7B-Instruct-Turbo
# Get key: https://api.together.ai → Settings → API Keys
#
# LLM_CLOUD_BASE_URL=https://api.together.xyz
# LLM_CLOUD_API_KEY=your-together-api-key-here
# LLM_CLOUD_DEFAULT_MODEL=meta-llama/Llama-3.1-8B-Instruct-Turbo
# LLM_CLOUD_PLANNER_MODEL=Qwen/Qwen2.5-7B-Instruct-Turbo
# LLM_CLOUD_SYNTHESIZER_MODEL=Qwen/Qwen2.5-7B-Instruct-Turbo
# LLM_CLOUD_REVIEWER_MODEL=Qwen/Qwen2.5-7B-Instruct-Turbo
# LLM_CLOUD_WRITER_MODEL=meta-llama/Llama-3.1-8B-Instruct-Turbo
# LLM_CLOUD_TIMEOUT=60.0

# ── Fireworks AI ──────────────────────────────────────────────────────────────
# Models: accounts/fireworks/models/llama-v3p1-8b-instruct
# Get key: https://fireworks.ai/account/api-keys
#
# LLM_CLOUD_BASE_URL=https://api.fireworks.ai/inference
# LLM_CLOUD_API_KEY=your-fireworks-api-key-here
# LLM_CLOUD_DEFAULT_MODEL=accounts/fireworks/models/llama-v3p1-8b-instruct
# LLM_CLOUD_PLANNER_MODEL=accounts/fireworks/models/qwen2p5-7b-instruct
# LLM_CLOUD_SYNTHESIZER_MODEL=accounts/fireworks/models/qwen2p5-7b-instruct
# LLM_CLOUD_REVIEWER_MODEL=accounts/fireworks/models/qwen2p5-7b-instruct
# LLM_CLOUD_WRITER_MODEL=accounts/fireworks/models/llama-v3p1-8b-instruct
# LLM_CLOUD_TIMEOUT=60.0

# ── Google Sign-In (production login) ─────────────────────────────────────────
# Required for real user accounts on the VM. After editing, redeploy:
#   ./scripts/deploy_prod.sh
#
# 1. Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client
# 2. Application type: Web application
# 3. Authorized JavaScript origins — add EXACTLY how users open the app, e.g.:
#      http://YOUR_VM_IP:8000
#      https://curai.yourdomain.com
# 4. Authorized redirect URIs: not required for @react-oauth/google One Tap/button flow
#
# AUTH_DISABLED=false
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# JWT_SECRET=generate-a-long-random-string-at-least-32-chars
# JWT_EXPIRE_MINUTES=10080
#
# Public URL users type in the browser (used for CORS when frontend/API differ):
# Include capacitor://localhost,ionic://localhost when testing bundled iOS/Android shells.
# CORS_ORIGINS=http://YOUR_VM_IP:8000,https://curai.yourdomain.com,capacitor://localhost,ionic://localhost
#
# Portfolio embed demo (iframe on your personal site)
# DEMO_ENABLED=true
# DEMO_MAX_QUESTIONS=5
# DEMO_FULL_APP_URL=https://app.cura-i.com
# DEMO_CONTEXT_MAX_CHARS=12000
# DEMO_EMBED_ALLOWED_ORIGINS=https://yourname.dev,https://www.yourname.dev
# DEMO_INTRO=Ask about my work in AI, healthcare engineering, academics, or cricket — or try a live weather/FX question.

# TAVILY_API_KEY=
# PERPLEXITY_API_KEY=
# FIRECRAWL_API_KEY=

# ── HTTPS subdomains (Caddy + Let's Encrypt on prod VM) ───────────────────────
# Enables https://grafana.app.cura-i.com with Grafana login (no SSH tunnel).
# One-time: DNS A record grafana.app.cura-i.com → VM IP, then on the server:
#   ./scripts/setup_grafana_subdomain.sh && ./scripts/deploy_prod.sh
#
# CADDY_APP_DOMAIN=app.cura-i.com
# CADDY_GRAFANA_DOMAIN=grafana.app.cura-i.com
# CADDY_ACME_EMAIL=you@example.com
# GRAFANA_ROOT_URL=https://grafana.app.cura-i.com
# GRAFANA_DOMAIN=grafana.app.cura-i.com
# GRAFANA_ADMIN_PASSWORD=change-me-strong-password